/**
 * Base CRUD Service for database operations
 * Handles all Create, Read, Update, Delete operations for CMS collections
 */

import { BaseCrudService as WixBaseCrudService } from '@/integrations';

export const BaseCrudService = WixBaseCrudService;
